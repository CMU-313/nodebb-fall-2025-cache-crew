config:
  target: "http://127.0.0.1:4567"
  phases:
    - duration: 10
      arrivalRate: 10
scenarios:
  - name: "Home and recent"
    flow:
      - get:
          url: "/"
      - get:
          url: "/recent"
